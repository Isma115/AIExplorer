# Backend

La carpeta `backend` queda reservada para la futura integracion de base de datos y ORM en `Node.js`.

Mientras no exista persistencia real, no debe contener logica de interfaz ni gestion funcional del chat.
